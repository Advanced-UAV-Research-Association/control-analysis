{
  "name": "Python Controls",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": []
    }
  },
  "postCreateCommand": "pip3 install --upgrade pip setuptools wheel && pip3 install -r requirements.txt"
}

